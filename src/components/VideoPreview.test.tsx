import { render, screen } from "@testing-library/react";import { describe, expect, it } from "vitest";import VideoPreview from "./VideoPreview";
describe("VideoPreview",()=>{it("exposes a descriptive play control when video exists",()=>{render(<VideoPreview title="Demo" poster="/poster.png" source="/demo.mp4"/>);expect((screen.getByRole("button",{name:"Play Demo walkthrough"}) as HTMLButtonElement).disabled).toBe(false)});it("keeps a missing-video preview non-interactive",()=>{render(<VideoPreview title="Static" poster="/poster.png"/>);expect((screen.getByRole("button",{name:"Static preview"}) as HTMLButtonElement).disabled).toBe(true)})});
