import { useEffect, useState } from "react";
import { Link, NavLink, Outlet, useLocation } from "react-router-dom";
import { PROFILE } from "../data/portfolio";
const nav = [["/", "Work"], ["/work/meridian", "Meridian"], ["/work/carryover", "Carryover"], ["/resume", "Résumé"]] as const;
export default function SiteShell() { const [open, setOpen] = useState(false); const location = useLocation(); useEffect(() => { setOpen(false); window.scrollTo({ top: 0, behavior: "auto" }); }, [location.pathname]); return <div className="site-shell"><a className="skip-link" href="#main-content">Skip to content</a><header className="site-header"><Link to="/" className="wordmark" aria-label="Justice Brutus, home"><span>Justice Brutus</span><small>Front-End Engineer</small></Link><button className="nav-toggle" type="button" aria-expanded={open} aria-controls="primary-nav" onClick={() => setOpen((value) => !value)}>{open ? "Close" : "Menu"}</button><nav id="primary-nav" aria-label="Primary" className={open ? "site-nav is-open" : "site-nav"}>{nav.map(([to, label]) => <NavLink key={to} to={to} end={to === "/"} className={({ isActive }) => isActive ? "is-active" : undefined}>{label}</NavLink>)}<a href={`mailto:${PROFILE.email}`} className="nav-contact">Email me</a></nav></header><main id="main-content"><Outlet /></main><footer className="site-footer"><div><p className="eyebrow">Available for product teams</p><h2>Clear products start with clear decisions.</h2></div><div className="footer-actions"><a href={`mailto:${PROFILE.email}`}>{PROFILE.email}</a><a href={PROFILE.github} target="_blank" rel="noreferrer">GitHub ↗</a><span>{PROFILE.location}</span></div></footer></div>; }
